<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.hapleow.hapcodeweb.dao.TableInfoMapper">


    <select id="getTableFields" resultType="top.hapleow.hapcodecore.model.TableField">

      ${sql}

    </select>

    <select id="getTables" resultType="top.hapleow.hapcodecore.model.TableInfo">

        ${sql}

    </select>
</mapper>
